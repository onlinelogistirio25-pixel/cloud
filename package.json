{
  "name": "logistiko-cloud",
  "version": "1.0.0",
  "description": "Simple demo cloud for per-client uploads",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
  "pm2:start": "npx pm2 start server.js --name logistiko-cloud --watch",
  "pm2:stop": "npx pm2 stop logistiko-cloud || true",
  "pm2:restart": "npx pm2 restart logistiko-cloud",
  "pm2:save": "npx pm2 save",
    "dev": "nodemon server.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "bcrypt": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "multer": "^1.4.5-lts.1",
    "sqlite": "^4.1.2",
    "sqlite3": "^5.1.6",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22",
    "pm2": "^6.0.10"
  }
}
